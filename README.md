# David Green — Enterprise AI Operations Portfolio

A self-contained static website for GitHub Pages.

## Files
- `index.html` — the full interactive portfolio
- `.nojekyll` — tells GitHub Pages to serve files directly

## Publish with GitHub Pages
1. Create a new public repository, for example `ai-operations-portfolio`.
2. Upload `index.html` and `.nojekyll` to the repository root.
3. Open **Settings → Pages**.
4. Under **Build and deployment**, select **Deploy from a branch**.
5. Choose the `main` branch and the `/ (root)` folder, then save.
6. GitHub will display the published URL after deployment.

## Edit
Open `index.html` in any text editor. Search for the text you want to change, edit it, and upload/commit the revised file.

## Recommended next additions
- A professional headshot
- A downloadable PDF résumé
- Screenshots or mockups of the three Claude deployments
- Case-study pages
- A custom domain such as `portfolio.cqi360.com`
